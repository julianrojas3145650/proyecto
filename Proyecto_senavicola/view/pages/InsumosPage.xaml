<Page x:Class="Proyecto_senavicola.view.pages.InsumosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:Proyecto_senavicola.view.controls"
      Title="Gestión de Insumos"
      Background="#F5F5F5"
      Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#179002"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1FA303"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="ColumnHeaderHeight" Value="50"/>
        </Style>

        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- NavBar -->
        <controls:NavBarControl x:Name="navBar" Grid.Row="0"/>

        <!-- Contenido Principal -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Margin="20">
            <StackPanel>

                <!-- Botones de Acción -->
                <Border Margin="10,10,10,5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="📦 Registrar Insumo" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="BtnRegistrarInsumo_Click" 
                        Margin="0,0,10,0"/>
                        <Button Content="📊 Ver Historial" 
                        Background="#2196F3"
                        Foreground="White"
                        Padding="15,10"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="BtnVerHistorial_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="8" 
                                    Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Estadísticas -->
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="🥣" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Alimento Total" FontSize="12" Foreground="#999" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtTotalAlimento" Text="0 kg" 
                                       FontSize="24" FontWeight="Bold" Foreground="#4CAF50" 
                                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="🧰" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Herramientas" FontSize="12" Foreground="#999" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtTotalHerramientas" Text="0" 
                                       FontSize="24" FontWeight="Bold" Foreground="#2196F3" 
                                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="💊" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Medicamentos" FontSize="12" Foreground="#999" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtTotalMedicamentos" Text="0" 
                                       FontSize="24" FontWeight="Bold" Foreground="#9C27B0" 
                                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="3" Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="⚠️" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Bajo Stock" FontSize="12" Foreground="#999" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtAlertasBajoStock" Text="0" 
                                       FontSize="24" FontWeight="Bold" Foreground="#f44336" 
                                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Filtros y Búsqueda -->
                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ComboBox x:Name="CmbFiltroTipo" Grid.Column="0"
                                  Height="40" FontSize="14" Margin="0,0,10,0"
                                  SelectionChanged="CmbFiltroTipo_SelectionChanged">
                            <ComboBoxItem Content="Todos los insumos" IsSelected="True"/>
                            <ComboBoxItem Content="🥣 Alimentos"/>
                            <ComboBoxItem Content="🧰 Herramientas"/>
                            <ComboBoxItem Content="💊 Medicamentos"/>
                            <ComboBoxItem Content="🛠️ Utensilios"/>
                            <ComboBoxItem Content="📦 Otros"/>
                        </ComboBox>

                        <Grid Grid.Column="1" Margin="0,0,10,0">
                            <TextBox x:Name="TxtBuscar"
                                     Height="40"
                                     FontSize="14"
                                     VerticalContentAlignment="Center"
                                     TextChanged="TxtBuscar_TextChanged"/>

                            <TextBlock x:Name="PlaceholderBuscar"
                                       Text="🔍 Buscar insumo..."
                                       Margin="10,0,0,0"
                                       VerticalAlignment="Center"
                                       Foreground="Gray"
                                       IsHitTestVisible="False"/>
                        </Grid>



                        <Button Grid.Column="2" Content="Buscar"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Click="BtnBuscar_Click"/>
                    </Grid>
                </Border>

                <!-- Tabla de Insumos -->
                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Inventario de Insumos" 
                                       FontSize="18" FontWeight="SemiBold"/>
                        </Grid>

                        <DataGrid Grid.Row="1" x:Name="DgInsumos" Style="{StaticResource ModernDataGrid}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoDisplay}" Width="120"/>
                                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="100"/>
                                <DataGridTextColumn Header="Unidad" Binding="{Binding Unidad}" Width="80"/>
                                <DataGridTextColumn Header="Fecha Ingreso" Binding="{Binding FechaIngreso, StringFormat=dd/MM/yyyy}" Width="120"/>
                                <DataGridTextColumn Header="Responsable" Binding="{Binding Responsable}" Width="120"/>
                                <DataGridTemplateColumn Header="Estado" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding EstadoColor}" 
                                                    CornerRadius="12" Padding="8,4">
                                                <TextBlock Text="{Binding EstadoTexto}" 
                                                           Foreground="White" FontSize="11" 
                                                           FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Acciones" Width="200">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Content="➕" 
                                                        Background="#4CAF50" 
                                                        Foreground="White"
                                                        Padding="8,4" 
                                                        Margin="5"
                                                        BorderThickness="0"
                                                        Click="BtnReabastecer_Click"
                                                        Tag="{Binding}"
                                                        ToolTip="Reabastecer"
                                                        Cursor="Hand">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="5" 
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                                <Button Content="✏️" 
                                                        Background="#2196F3" 
                                                        Foreground="White"
                                                        Padding="8,4" 
                                                        Margin="5"
                                                        BorderThickness="0"
                                                        Click="BtnEditarInsumo_Click"
                                                        Tag="{Binding}"
                                                        ToolTip="Editar"
                                                        Cursor="Hand">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="5" 
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                                <Button Content="🗑️" 
                                                        Background="#f44336" 
                                                        Foreground="White"
                                                        Padding="8,4"
                                                        Margin="5"
                                                        BorderThickness="0"
                                                        Click="BtnEliminarInsumo_Click"
                                                        Tag="{Binding}"
                                                        ToolTip="Eliminar"
                                                        Cursor="Hand">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                    CornerRadius="5" 
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>