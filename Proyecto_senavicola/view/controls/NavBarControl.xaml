<UserControl x:Class="Proyecto_senavicola.view.controls.NavBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="80"
             Background="White">

    <UserControl.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                    <Setter Property="Foreground" Value="#179002"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
        <Grid Margin="20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Sección izquierda: Título del módulo -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock x:Name="txtModulo" 
                           Text="Módulo" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#179002"/>
                <TextBlock x:Name="txtDescripcion" 
                           Text="" 
                           FontSize="12" 
                           Foreground="#999999" 
                           Margin="0,2,0,0"/>
            </StackPanel>

            <!-- Sección derecha: Información del usuario y acciones -->
            <StackPanel Grid.Column="1" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center">

                <!-- Información del usuario -->
                <Button Background="Transparent"
                        BorderThickness="0"
                        BorderBrush="Transparent"
                        Click="BtnPerfilUsuario_Click"
                        Cursor="Hand">

                    <!-- Quitar hover, bordes y efectos del botón -->
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter />
                        </ControlTemplate>
                    </Button.Template>

                    <!-- Tu contenido -->
                    <Border Background="#F5F5F5" 
                            CornerRadius="20" 
                            Padding="15,8" 
                            Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👤" 
                                       FontSize="16" 
                                       VerticalAlignment="Center" 
                                       Margin="0,0,8,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock x:Name="txtUsuarioNombre" 
                                           Text="Usuario" 
                                           FontSize="13" 
                                           FontWeight="SemiBold" 
                                           Foreground="#333333"/>
                                <TextBlock x:Name="txtUsuarioRol" 
                                           Text="Rol" 
                                           FontSize="10" 
                                           Foreground="#999999"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                </Button>



                <!-- Botones de acción -->
                <Button x:Name="btnNotificaciones" 
                        Style="{StaticResource NavButtonStyle}"
                        ToolTip="Notificaciones"
                        Click="BtnNotificaciones_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔔" FontSize="16"/>
                        <Border x:Name="badgeNotificaciones" 
                                Background="#F44336" 
                                CornerRadius="10" 
                                Padding="5,2" 
                                Margin="2,-8,0,0"
                                Visibility="Collapsed"
                                VerticalAlignment="Top">
                            <TextBlock x:Name="txtCountNotificaciones" 
                                       Text="0" 
                                       FontSize="10" 
                                       FontWeight="Bold" 
                                       Foreground="White"/>
                        </Border>
                    </StackPanel>
                </Button>

                <Button x:Name="btnAyuda" 
                        Content="❓" 
                        Style="{StaticResource NavButtonStyle}"
                        ToolTip="Ayuda"
                        Click="BtnAyuda_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
