<Window x:Class="Proyecto_senavicola.view.dialogs.HistorialInsumosDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Historial de Movimientos" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">

    <Border CornerRadius="15" Background="White" BorderBrush="#2196F3" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#2196F3" CornerRadius="15,15,0,0">
                <Grid>
                    <TextBlock Text="📋 Historial de Movimientos de Insumos" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               VerticalAlignment="Center" 
                               Margin="20,0"/>
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center" 
                            Width="40" 
                            Height="40" 
                            Background="Transparent" 
                            Foreground="White" 
                            BorderThickness="0" 
                            FontSize="20" 
                            Cursor="Hand" 
                            Click="BtnCerrar_Click" 
                            Margin="0,0,10,0"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <ComboBox x:Name="cmbFiltroTipo" 
                          Grid.Row="0" 
                          Height="40" 
                          Margin="0,0,0,15"
                          SelectionChanged="CmbFiltro_SelectionChanged">
                    <ComboBoxItem Content="Todos los movimientos" IsSelected="True"/>
                    <ComboBoxItem Content="Entradas"/>
                    <ComboBoxItem Content="Salidas"/>
                    <ComboBoxItem Content="Ajustes"/>
                </ComboBox>

                <DataGrid x:Name="dgMovimientos" 
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          BorderThickness="1"
                          BorderBrush="#E0E0E0"
                          AlternatingRowBackground="#F9F9F9"
                          RowHeight="45">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FechaMovimiento, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                        <DataGridTextColumn Header="Insumo" Binding="{Binding NombreInsumo}" Width="*"/>
                        <DataGridTemplateColumn Header="Tipo" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding TipoColor}" 
                                            CornerRadius="12" 
                                            Padding="8,4">
                                        <TextBlock Text="{Binding TipoMovimiento}" 
                                                   Foreground="White" 
                                                   FontWeight="SemiBold" 
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="100"/>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding UsuarioNombre}" Width="150"/>
                        <DataGridTextColumn Header="Motivo" Binding="{Binding Motivo}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Border Grid.Row="2" Background="#F5F5F5" Padding="20" CornerRadius="0,0,15,15">
                <Button Content="Cerrar" 
                        Width="100" 
                        Height="35" 
                        Background="#666666" 
                        Foreground="White" 
                        BorderThickness="0" 
                        Cursor="Hand"
                        HorizontalAlignment="Right"
                        Click="BtnCerrar_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="8" 
                                    Padding="15,8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>