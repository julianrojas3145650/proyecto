<Window x:Class="Proyecto_senavicola.view.dialogs.SeleccionFormatoReporteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seleccionar Formato" 
        Height="400" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize">

    <Border CornerRadius="15" Background="White" BorderBrush="#179002" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Cabecera -->
            <Border Grid.Row="0" Background="#179002" CornerRadius="15,15,0,0">
                <Grid>
                    <TextBlock Text="📊 Seleccionar Formato de Reporte" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               VerticalAlignment="Center" 
                               Margin="20,0"/>
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center" 
                            Width="40" 
                            Height="40" 
                            Background="Transparent" 
                            Foreground="White" 
                            BorderThickness="0" 
                            FontSize="20" 
                            Cursor="Hand" 
                            Click="BtnCancelar_Click" 
                            Margin="0,0,10,0"/>
                </Grid>
            </Border>

            <!-- Contenido -->
            <StackPanel Grid.Row="1" Margin="30,30">
                <TextBlock x:Name="txtNombreReporte" 
                           Text="Reporte" 
                           FontSize="16" 
                           FontWeight="SemiBold" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,30"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Opción PDF -->
                    <Button Grid.Column="0" 
                            x:Name="btnPDF" 
                            Height="150" 
                            Background="White" 
                            BorderBrush="#E0E0E0" 
                            BorderThickness="2" 
                            Cursor="Hand"
                            Click="BtnPDF_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="10" 
                                        Padding="20">
                                    <StackPanel>
                                        <TextBlock Text="📄" 
                                                   FontSize="48" 
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Text="PDF" 
                                                   FontSize="18" 
                                                   FontWeight="Bold" 
                                                   HorizontalAlignment="Center" 
                                                   Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFEBEE"/>
                                        <Setter Property="BorderBrush" Value="#DC3545"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Opción Excel -->
                    <Button Grid.Column="2" 
                            x:Name="btnExcel" 
                            Height="150" 
                            Background="White" 
                            BorderBrush="#E0E0E0" 
                            BorderThickness="2" 
                            Cursor="Hand"
                            Click="BtnExcel_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="10" 
                                        Padding="20">
                                    <StackPanel>
                                        <TextBlock Text="📊" 
                                                   FontSize="48" 
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Text="Excel" 
                                                   FontSize="18" 
                                                   FontWeight="Bold" 
                                                   HorizontalAlignment="Center" 
                                                   Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E8F5E9"/>
                                        <Setter Property="BorderBrush" Value="#4CAF50"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </StackPanel>

            <!-- Pie -->
            <Border Grid.Row="2" Background="#F5F5F5" Padding="20" CornerRadius="0,0,15,15">
                <Button Content="Cancelar" 
                        Width="100" 
                        Height="35" 
                        Background="#666666" 
                        Foreground="White" 
                        BorderThickness="0" 
                        Cursor="Hand"
                        HorizontalAlignment="Center"
                        Click="BtnCancelar_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="8" 
                                    Padding="15,8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>